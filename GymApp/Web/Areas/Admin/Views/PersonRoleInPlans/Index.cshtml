@model IEnumerable<Domain.PersonRoleInPlan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.RoleName.Translate()
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersonRoleInPlanId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersonRoleInPlanId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersonRoleInPlanId })
        </td>
    </tr>
}

</table>
